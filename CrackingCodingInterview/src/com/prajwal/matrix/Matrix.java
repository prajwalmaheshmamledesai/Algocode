package com.prajwal.matrix;

public class Matrix {

	/*
	 * 
	 * Constraint the nos int he path must be increasing by 1
	 */

	void matrixLongestPath(int[][] mat, int i, int j) {
		if (i < 0 && j < 0)
			return;
		if (i == 0 && j == 0)
			return;

	}

}
